# Server credentials (copy to .env — never commit .env)
FRP_TOKEN=
DASHBOARD_USER=admin
DASHBOARD_PASS=
ACME_EMAIL=you@example.com

# Optional overrides
# DOMAIN=tunnel.nexorylabs.com
# FRP_VERSION=0.69.1
# TCP_PORT_START=20000
# TCP_PORT_END=30000
