#!/bin/sh
set -e

case "$1" in
  configure)
    echo ""
    echo "nexory-tunnel installed."
    echo "  Run: nexory-tunnel login"
    echo "  Then: nexory-tunnel http 3000"
    echo ""
    ;;
esac

#DEBHELPER#
exit 0
